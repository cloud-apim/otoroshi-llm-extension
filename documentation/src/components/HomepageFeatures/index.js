// This component is no longer used on the homepage.
// Features are now rendered directly in src/pages/index.js.
// Kept for backward compatibility in case it is referenced elsewhere.

export default function HomepageFeatures() {
  return null;
}
